# BisunERP v2.0

## 발주 전 안전검사

- 수령인, 연락처, 주소, 공급처, 상품매핑, 발주수량 필수 검사
- 공급처 상품코드, 우편번호, 매입단가 누락 주의 표시
- 오류가 있으면 발주서 생성을 차단하고 수정 위치 안내
- 주의사항만 있으면 사용자가 확인 후 발주 가능

# BisunERP v1.8

## v1.6 - 공급처별 발주센터
- 발주대기 주문을 공급처별로 자동 집계
- 공급처별 주문상품 수, 발주수량, 예상금액, 발주차수 표시
- 공급처 한 곳만 선택해 발주서 생성
- 공급처 선택 시 상세 주문목록 자동 필터
- 기존 전체 발주/선택 발주/발주서 폴더 열기 유지

# 비선상회 ERP v1.3

## 이번 버전
- Windows 관리자 화면(GUI) 추가
- 버튼으로 쿠팡 주문 처리 및 공급처별 발주서 생성
- 주문 파일, 발주 결과, 상품매핑표, 미매핑 목록, 처리 이력 바로 열기
- v1.1의 주문 누적관리 및 중복 발주 차단 유지
- 콘솔 실행 방식도 `콘솔실행.bat`로 유지

## 실행 방법
1. `data` 폴더에 처리할 쿠팡 주문 엑셀 1개를 넣습니다.
2. `실행.bat`를 더블클릭합니다.
3. 관리자 화면에서 `주문 처리 및 발주서 생성` 버튼을 누릅니다.
4. 신규 상품이 있다면 `상품 매핑표 열기`를 눌러 공급처와 내부상품 정보를 입력합니다.
5. 저장 후 다시 주문 처리 버튼을 누릅니다.

## 주의
- 처리 전 `output` 폴더의 엑셀 파일을 닫아야 합니다.
- `상품매핑표.xlsx`와 `주문처리이력.xlsx`는 운영 데이터이므로 삭제하지 마세요.
- 같은 주문은 주문처리이력을 기준으로 중복 발주되지 않습니다.


## v1.5 - 주문 등록 후 바로 발주

- 쿠팡 신규 주문 등록 직후 자동매핑 결과를 확인합니다.
- 이번 업로드 주문 중 상품과 공급처가 연결된 항목만 발주 가능 수량으로 표시합니다.
- 미매핑/중복 후보가 없으면 사용자 확인 후 이번 주문만 공급처별 발주서로 즉시 생성할 수 있습니다.
- 미매핑 항목이 있으면 발주하지 않고 확인 대상 수를 안내합니다.


## v1.8 - 신규 주문 전용 자동매핑 최적화

- 주문 엑셀 등록 후 이번에 생성된 주문만 자동매핑
- 기존 전체 미매핑 주문 재검사 제거
- 신규 주문에도 저장된 영구 매핑 규칙 정상 적용
- 주문별 매핑 결과 합산 표시

## v1.7 - 발주센터 안정화

- 발주관리 화면의 KPI 오류 수정
- 발주대기 상품의 매입단가·상품금액·택배비·택배사 표시 연결
- 발주완료 저장 시 공급처 상품코드와 금액 정보를 함께 저장
- v1.6에서 발생하던 `get_dashboard_summary` 누락 오류 해결
